# darkmem mirror for dr-superpowers

**Source:** darkmem register `docs/superpowers/registers/2026-09-23-work-log-lane.md` rows #3 ("they usually come with ledger and progress tracking documents") and #5 ("integrate with dr-superpowers to store the documents and progress tracking, reduce the repo pollution"), owner requests 2026-09-23
**Covers:** docs/superpowers/specs/2026-09-23-darkmem-mirror-design.md, docs/superpowers/specs/2026-09-25-darkmem-sync-hardening-design.md

| # | Item | Assigned | Acceptance | State | Note |
|---|---|---|---|---|---|
| 1 | darkmem-sync: config and modes, the mirror, pull, push, status and import over darkmem's keyed REST surface (spec §1, §3, §7) | docs/superpowers/plans/2026-09-24-darkmem-sync-client.md | tests/darkmem-sync.test.sh green; validate-repository and claude plugin validate pass; local mode unchanged | done | increment 1 merged into docs/darkmem-mirror-spec at 2965b67; a red Windows shard on row 6 reopens it |
| 2 | Resolve every docs/superpowers and .superpowers reference through sp_docs_root / sp_work_root, including how a plan outside the repository is identified (spec §2) | - | - | open | needs a ruling: plan_require_same_repo, ledger identity lines, register Covers paths and plans/completed.md assume repository-relative plan paths; also: the spec never says what a deletion does (a document deleted in the mirror is fetched again by pull, one deleted on darkmem stays in the mirror, and distilling-docs deletes what it absorbs), and pathToUri takes the leftmost superpowers/ segment so a project or checkout named superpowers doubles the uri |
| 3 | Run pull at SessionStart and push at the skills' stop points, with a Stop hook as the Claude Code safety net (spec §4) | - | - | open | the handoff skill must define the literal - Draft: line that checkpointTarget requires for a design-phase latest.md (skills/handoff/SKILL.md names only the plan line); pass DR_SUPERPOWERS_SESSION_ID from the hook, since runKey mismatches session-start.sh for non-ASCII paths and reads stale session records |
| 4 | Skills record durable facts with memory_add at task-complete and handoff (spec §5) | - | - | open | - |
| 5 | Move a repository into darkmem: mapping, import, round-trip check, delete docs/superpowers in its own commit; darkmem's CLAUDE.md citations first (spec §6) | - | - | open | owner-run, after rows 1-3 land and darkmem increment 1 is deployed; import closes every workstream it files and pull renders only open ones, so a plan in progress at the move cannot resume from the mirror: import only when no plan is in progress, or make pull render a closed workstream the mirror lacks; import also overwrites documents darkmem already holds and skips dotfiles and symlinks without a note |
| 6 | darkmem-sync on Windows: CI's six Windows shards green at the release commit | - | - | verify | the suite has run on Linux only; validate.yml also runs on pull_request, so merge through a pull request and treat its six Windows shards as the merge gate; a red Windows shard reopens row 1 |
| 7 | darkmem-sync hardening left open by the final review of increment 1 | docs/superpowers/plans/2026-09-26-darkmem-sync-hardening.md | the hardening spec's §2-§4 hold against the stub: one failed item never stops push or pull, ledgers push under expected_last_seq, failed handoff links retry, invalid ledger bytes fail with their offset, checkpoints compare to the microsecond, unportable and case-twin uris are refused, and the lock never deletes a lock it did not judge abandoned; darkmem-sync suite green | doing | C5 and C11 first: they must land before row 3's Stop hook pushes unattended, and a document error should name its uri; one bad document (5xx or 403) stops push before ledgers and the checkpoint; a ledger with an invalid UTF-8 byte stalls silently at exit 0; uris with : * ? fail on Windows and case-only uris collide; checkpoint times compare at millisecond precision; a failed handoff link is retried only by a later append; the lock takeover among three processes and two mirrors appending the same ledger suffix at once need an expected-sequence precondition on darkmem's append route, which darkmem's own register does not yet list |
| 8 | Codex-executed commits 2c412d6 and 151cf12 carry no Co-Authored-By trailer | - | - | deferred | the Codex wrapper writes the commit message; no Codex trailer format exists in this repository's history; Task 1 and Task 3 minor |
| 9 | darkmem-config: primaryRoot returns null inside a submodule or on git older than 2.31, and samePath does not realpath the config keys | - | - | deferred | both fall back to local mode with a misleading reason, which is safe; Task 1 minors (darkmem-config.mjs) |
| 10 | darkmem-mirror: propertiesFor swallows every error, splitAtLimit(text, 1) loops on a high surrogate, loadState reads an inaccessible state file as a fresh start | - | - | deferred | unreachable or harmless at the shipped defaults; Task 3 minors |
| 11 | darkmem-mirror tests: the reserved and never-synced branches of localChanges, the pending ledger flag and writeFileAtomic have no direct assertions | - | - | deferred | exercised indirectly by the push and pull suites; Task 3 minor |
| 12 | push: state.checkpointAt is global, not per workstream, and a push with new ledger bytes re-reads the whole ledger and every changed latest.md reads its checkpoint history | - | - | deferred | the checkpointAt edge needs a lost answer, a push to another workstream and a revert together; the reads are a cost the ruling accepted; Task 6 minors |
| 13 | import: a 422-refused document or ledger is reported twice and its failed run leaves one darkmem-import-* scratch directory | - | - | deferred | output noise; the exit code stays 1; Task 7 minor |
| 14 | darkmem's work-log register needs a row asking for an expected-sequence precondition on the append route | darkmem: docs/superpowers/plans/2026-09-25-worklog-append-precondition.md | darkmem's append refuses a stale expected_last_seq on both doors (409 / seq_conflict) and answers last_seq | verify | merged into darkmem master with both doors (darkmem register row 26, 2026-09-25), not deployed; check after the owner deploys it |
| 15 | acquireLock's restore can replace a third process's fresh lock: on POSIX, rename onto an existing empty directory replaces it, so a restore landing between that process's mkdir and its owner.json write takes its place | docs/superpowers/plans/2026-09-26-darkmem-sync-hardening.md | off Windows a live lock directory is never empty (built under a temporary name, released by moving it aside), so no restore or new lock's rename can replace a fresh lock; the mirror suite covers the restore path and leftover cleanup | doing | - |
