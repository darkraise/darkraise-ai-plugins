# Normalize line endings: store text as LF in the repo and check out LF on all
# platforms, so shell scripts and manifests stay consistent for the Ubuntu CI
# runner and every contributor regardless of core.autocrlf.
* text=auto eol=lf
docs/superpowers/plans/completed.md merge=union
